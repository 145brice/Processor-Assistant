streamlit
pypdf
python-dotenv
thefuzz
python-docx
cryptography
# Supabase sync uses the REST API via stdlib urllib to avoid client/httpx drift.

fastapi
uvicorn[standard]

# Optional: only needed when GEMINI_USE_VERTEX is enabled (Vertex AI / ZDR path).
google-auth

# Optional local OCR path for scanned PDFs. Tesseract desktop must also be installed.
pymupdf
pytesseract
Pillow
